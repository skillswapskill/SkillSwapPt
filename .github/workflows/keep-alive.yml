name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"   # Every 10 minutes
  workflow_dispatch:         # allows manual run

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl SkillSwap site
        run: |
          curl -s https://skillswap.company > /dev/null
